# SuperPicky macOS dependency set (Apple Silicon arm64 / Intel x86_64)
# Uses standard PyPI torch (supports MPS/Metal on Apple Silicon)

-r requirements_base.txt

torch==2.8.0
torchvision
# torchaudio 已移除：全项目零 import，PyInstaller 不会打包它，只是白装。
# torchaudio removed: never imported, never bundled by PyInstaller.

pyinstaller
huggingface_hub
lap
