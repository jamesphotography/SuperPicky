# Shared dependencies for SuperPicky (CPU and CUDA builds)

# 锁定版本：未锁时 CI 悄悄升到 8.4.x，其全局 Image.open 补丁引出 NEF 识鸟拉起新窗口的缺陷。升级须先验证。
# Pinned: unpinned CI silently moved to 8.4.x whose global Image.open patch caused a regression. Validate before bumping.
ultralytics==8.4.147
opencv-python>=4.0.0
rawpy>=0.18.0
imageio>=2.0.0
numpy>=1.20.0
Pillow>=9.0.0
pillow-heif>=0.10.0
pi-heif
PySide6>=6.6.0

timm>=0.9.0
cryptography>=41.0.0

flask>=3.1.2
flask_cors>=6.0.2

imagehash

tzdata

httpx>=0.27.0
huggingface_hub>=0.23.0

# CACNet 构图裁剪网络依赖 / Required by CACNet composition-aware cropping model
